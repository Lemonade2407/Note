# 陈睿

15600475530 |  lemonade051112@outlook.com | github.com/Lemonade2407
**在读 | 意向：Java 后端实习生 | 北京**

---

## 教育经历

**北京工业大学**  
软件工程 本科 | 全日制 计算机学院 | 2024.09 - 2028.06  
学分加权平均分：**87.96/100** | CET-4：523 | CET-6：510

---

## 专业技能

- **Java**：扎实掌握 OOP、设计模式（策略/工厂/模板），熟悉 Java 21 特性（虚拟线程、Sequenced Collections）
- **Spring Boot 3.x**：熟练开发 RESTful API，掌握拦截器、全局异常处理、AOP，能通过**自定义注解 `@RequireRole`** 实现角色级权限控制（引导问：AOP 切面执行顺序、注解解析原理）
- **MySQL**：掌握索引优化（覆盖索引、最左前缀原则）、通过 `EXPLAIN` 分析 `ref/type/extra` 字段定位慢查询，有联合索引设计经验（引导问：索引失效场景、回表与索引下推）
- **MyBatis + PageHelper**：熟练使用，能手写复杂动态 SQL，理解分页插件底层拦截器原理
- **缓存**：使用 **Caffeine** 实现热点数据缓存，熟悉 `expireAfterWrite`、`refreshAfterWrite` 策略及缓存穿透/击穿应对（引导问：Caffeine 与 Redis 使用场景区别、W-TinyLFU 算法）
- **认证与安全**：基于 **JWT + BCrypt** 实现无状态认证，了解 JWT 三段式结构（Header/Payload/Signature）及 BCrypt 加盐存储机制（引导问：JWT 如何续签、注销时的黑名单方案）
- **部署与运维**：使用 Docker Compose + Nginx 容器化部署，编写 Linux/Windows 双平台一键部署脚本，熟悉 Nginx 反向代理与日志监控
- **版本控制**：熟悉 **Git Feature 分支工作流**，掌握 `rebase` / `merge` 差异及冲突解决策略
- **其他**：集成过阿里云 OSS（文件上传/ZIP打包下载）、WebSocket（实时推送），了解 Vue 3 + Vite，能高效配合前端联调

---

## 项目经历

### ProjecTree 知享圈 | 独立开发者 & 运维负责人 *2025.03 - 至今*

🔗 [https://bjut-zxq.cn](https://bjut-zxq.cn/)

**项目概述**：独立开发并上线一个校内项目协作与组队平台，解决学生项目缺乏反馈、组队困难问题。采用 **Java 21 + Spring Boot 3.5 + MyBatis + Vue 3**，Docker 容器化部署，日均请求量 xxxx。

**具体工作与成果**：

- **细粒度权限控制**：自定义 `@RequireRole` 注解，通过 Spring AOP 实现学生/教师/管理员的接口拦截，**避免硬编码角色判断，权限校验代码复用率提升 80%**。
- **实时通知系统**：基于 **WebSocket** 搭建组队申请推送通道，相比前端轮询方案 **减少服务器带宽消耗约 xx**，消息延迟低于 xxx。
- **文件处理优化**：集成阿里云 OSS 实现项目文件上传与 ZIP 批量下载；利用 `mammoth` / `marked` 实现 Word/Markdown 在线预览，**用户无需下载即可查看，减少存储费用约 20%**。
- **数据库性能优化**：完成 11 张业务表设计，对高频查询（项目列表、组队申请）使用 `EXPLAIN` 分析并增加联合索引 **（`(status, create_time)`）**，**单次查询耗时从 xxxs 降至 xxxs**。
- **缓存策略**：使用 Caffeine 本地缓存热点数据（项目分类、用户基本信息），设置 `refreshAfterWrite=5min`，**热门数据缓存命中率达到 85%，接口平均响应时间降低 40%**。
- **自动化部署**：编写 Docker Compose + Nginx 配置及 Linux/Windows 双平台一键脚本，**实现“一行命令”完成环境初始化+服务启动，部署时长从 20 分钟压缩至 2 分钟**。

---
