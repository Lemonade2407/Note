# 数据结构概论
## 数据结构的概念
### 数据与数据结构
- 数据：信息的载体，式描述客观事物的数、字符，以及所有能输入计算机中并被计算机程序识别和处理的符号的集合。
	- 数值型数据：整数、浮点数、复数、双精度数
	- 非数值型数据：字符、字符串
- 数据元素是数据的基本单元，由若干的数据项组成
- 数据项：
	- 初等项：不可再分
	- 组合项：可再分
- 结构：数据元素间的关系
- 数据结构：由某一数据元素的集合和该集合中的数据元素之间的关系组成，记为Data_Structure={D,R}
### 数据结构的分类
- 线性结构
- 非线性结构
	- 层次结构
	- 群结构
# 线性表
## 线性表的概念
### 线性表的定义
- 线性表是一个有限序列
## 顺序表
- 顺序存储方式实现的线性表
- 存储利用率高、存取速度快
- 插入删除慢、需要预估大小
## 单链表
- 链接存储方式实现的线性表：链表
## 线性链表的其他变形
### 循环单链表
### 双向链表
# 栈和队列
## 栈
### 栈的定义
- 只允许在表的末端进行插入删除的线性表
- 后进先出（LIFO）
### 顺序栈
- 基于数组的存储实现
### 链式栈
- 基于链表的存储实现
## 队列
### 队列的概念
- 只允许在表的一端插入，在另一端删除
- 先进先出（FIFO）
### 循环队列
### 链式队列
## 优先级队列
- 按优先值排列的队列
## 双端队列
- 可以在队列的两端进行插入和删除
# 数组、串与广义表
## 多维数组
## 特殊矩阵
## 稀疏矩阵
## 字符串
## 广义表
- 简称表，是线性表的扩展；
- 记作$LS=(a_0,a_1,...,a_n)$
- 广义表允许表中有表
- 一般用链表结构作为存储表示
- 表节点组成：标志域+信息域+尾指针域
	- 标志域：
		- 0：头节点
		- 1：原子节点
		- 2：子表节点
	- 信息域：
		- 标志域为0时：引用计数
		- 标志域为1时：存放数据值
		- 标志域为2时：子表表头指针
	- 尾指针域：
		- 标志域0时：存放指向改表表头元素节点的指针
		- 标志域不为0时：存放下一个表节点的地址
# 树
## 树
### 自由树
- 成为连通图
### 有根树
## 二叉树

## 线索二叉树
- 原二叉链表中增加一个前驱指针域和一个后继指针域，指向某种次序下的前驱节点和后继节点
## 树与森林
### 树的存储表示
- 广义表表示法
- 父指针表示法：data域+parent域
- 子女链表表示法：data域+child1+child2+...+childn
- 子女-兄弟链表表示法：data域+firstChild+nextSibling
### 树、森林与二叉树的转换
- 树、森林转化为二叉树：对于每个节点，仅保留一个子女节点；断开的子节点连接在右兄弟上
- 二叉树转换为树和森林：斩断右孩子，右孩子连接上一个层次
# 集合与字典
## 集合及其表示
- 集合的成员是无序的、不重复的
- 位向量实现
- 有序链表实现
## 并查集与等价类
- 解决集合中元素划分的问题
- 并查集包含3中操作：
	- Union：合并
	- Find：查找
	- UFSets：初始化
- 并查集的下标代表元素名，内容代表对应的父节点地址
## 字典
- <key,value>对
- 三种组织方式：线性表、跳表、散列表
### 字典的线性表描述
- 有序顺序表、有序链表
## 跳表
- 在链表前中后或者1/4，3/4处增加连接指针，加快搜索速度![[跳表结构.png]]
## 散列（Hash table）
- Address=Hash(key)
- Hash函数：
	- 定义域必须包括需要存储的全部关键码
	- 结果域均匀分布到整个地址空间中
	- 函数必须是简单的，计算时间短
	- 除留余数法
	- 数字分析法
	- 平方取中法
	- 折叠法
- 处理冲突（溢出处理）
	- 线性探查法
	- 二次探查法
	- 双散列法
	- 
# 搜索结构
## 静态搜索
## 二叉树搜索
## AVL树
## 伸展树
## 红黑树
# 图
## 图的基本概念
## 图的存储结构
## 图的遍历
## 最小生成树
## 最短路径
## 用顶点表示活动的网络（AOV网络）
## 用边表示活动的网络（AOE网络）
# 排序
[1.2 数组排序 | 算法通关手册（LeetCode）](https://algo.itcharge.cn/01_array/01_02_array_sort/)
## 排序算法的选择策略
在实际应用中，选择合适的排序算法需要考虑以下因素：
1. 数据规模
    - **小规模数据**（n < 50）：可以使用简单排序算法，如插入排序
    - **中等规模数据**（50 ≤ n < 1000）：推荐使用快速排序或归并排序
    - **大规模数据**（n ≥ 1000）：优先考虑快速排序、归并排序或堆排序
2. 数据特征
    - **基本有序**：插入排序效率较高
    - **数据分布均匀**：快速排序效率较高
    - **数据范围较小**：计数排序或桶排序可能更高效
    - **数据有大量重复**：三路快速排序或计数排序更合适
3. 环境约束
    - **空间限制**：选择原地排序算法
    - **稳定性要求**：选择稳定排序算法
    - **硬件环境**：考虑缓存友好性和并行化潜力
4. 实际应用场景
    - **系统排序**：通常使用快速排序或归并排序的混合算法
    - **外部排序**：使用归并排序
    - **实时系统**：优先考虑时间复杂度稳定的算法
    - **内存受限环境**：选择空间复杂度低的算法
## 插入排序
```
void insertionSort(vector<int>& arr){

    for(int i=1;i<arr.size();i++){

        int temp=arr[i];

        int j=i-1;

        while (j >= 0 && arr[j] > temp) {

            arr[j + 1] = arr[j];

            j--;

        }

        arr[j + 1] = temp;

    }

}
```
## 快速排序
## 选择排序
## 归并排序
## 分配排序
# 文件、外部排序与搜索
## 外排序
## 多级索引结构
## 字典树