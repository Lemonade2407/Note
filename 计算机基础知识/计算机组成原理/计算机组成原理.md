# 计算机系统概论
## 计算机的基本组成
### 冯·诺依曼计算机
- 计算机由运算器、存储器、控制器、输入设备和输出设备五大部件组成。
- 指令和数据以同等地位存放千存储器内，并可按地址寻访。
- 指令和数据均用二进制数表示。
- 指令由操作码和地址码组成，操作码用来表示操作的性质，地址码用来表示操作数在存储器中的位置。
- 指令在存储器内按顺序存放。通常，指令是顺序执行的，在特定条件下，可根据运算结果 或根据设定的条件改变执行顺序。
- 机器以运算器为中心，输入输出设备与存储器间的数据传送通过运算器完成。
### 计算机系统五大功能部件
### 运算器
- 执行程序，进行算数、逻辑运算
#### 算术运算
- 加、减、乘、除、取模、自增、自减
#### 逻辑运算
- 与、或、非、异或、比较、移位
### 存储器
- 存放程序和数据，分为主存储器（内存）和辅助存储器（外存）
- 只能保存二进制数
- 内存：
	- 优点：访问速度快，直接和运算器交换信息，一般只储存正在运行程序和待处理数据
	- 缺点：容量小、无法断电保存数据
	- 由存储单元构成
		- 储存的二进制数称为存储字、位数称为存储字长
		- 每个存储单元都有地址，称为存储地址或内存地址
		- 准备访问储存单元时，需先将地址存在存储器地址寄存器（MAR）中，然后把数据暂存在存储器数据寄存器（MDR）中 
		- MAR位数表示max可寻址存储单元个数，MDR位数等于存储字长
- 外存：
	- 优点：容量大、可以断电保存数据
	- 缺点：无法直接和运算器交互，必须先把数据调入内存，访问速度慢
	- 一般认为是磁盘，由磁盘块构成
### 控制器
- 协调输入设备、输出设备、存储设备、运算器协同工作
### 输入设备
- 键盘、鼠标、扫描仪等
### 输出设备
- 显示器、打印机等
### 现代计算机
- 早期计算机运算器和控制器分离，以运算器为核心
![[冯·诺依曼计算机结构框图.png]]
- 现代计算机以存储器为核心，运算器和控制器集成为中央处理器（CPU）【CPU还集成了MAR和MDR】。中央处理器的核心部件是算术逻辑单元（ALU）和控制单元（CU），ALU完成运算，CU控制I/O设备、解释存储器指令并发出操作指令
- ![[以存储器为中心的计算机结构框图.png]]
### 存储程序工作方式
1. 将程序所含指令和所需数据从外存读入内存
2. CPU中程序计数器（PC）存放将执行的指令地址，执行前设置PC的值为指令1地址，(PC)=addr1;
3. PC把内容传给MAR，告知内存访问地址，(PC)->MAR
4. 内存获取MAR中的地址后，将对应存储单元的内容取出并放入MDR中，(M)->MDR
5. 取出指令1后，PC++
6. CPU中的指令寄存器（IR）用来存放当前指令，内容来自MDR，(MDR)->IR
7. 指令由操作码(OP)和操作数（数据或地址）构成，IR识别出OP后送给CU分析
8. CU控制内存取出对应数据到MDR中，将MDR中的数据读入通用寄存器组（GPRs），GPRs一般用来暂存ALU需要的数据
9. CU控制ALU进行操作，并将结果放回MDR
10. 根据MAR的地址，将MDR的数据存入内存
11. PC将内容送给MAR，开启新一轮指令执行
## 计算机硬件的主要技术指标
### 机器字长
CPU一次能处理数据的位数，通常与CPU的寄存器位数有关。字长越长，数的表示范围越大，精度也越高。机器的字长也会影响机器的运算速度。
### 存储容量
存储器的容量应该包括主存容量和辅存容量。
主存容量是指主存中存放二进制代码的总位数。 
存储容量＝存储单元个数 X 存储字长
辅存容量通常用字节数来表示。
### 运算速度
- 吉普森 (Gibson) 法：$T_M=\sum^n_{i=1}f_it_i$，$T_M$为机器运行速度；$f_i$, 为第 i 种指令占全部操作的百分比数；$t_i$为第 i 种指令的执行时间。 
- 单位时间内执行指令的平均条数，用 MIPS (Million In- struction Per Second ，百万条指令每秒）作为计量单位。
- CPI(Cycle Per Instruction) 即执行一条指令所需的时钟周期（机器主频的倒数）
- FLOPS(Floating Point Operation Per Second ，浮点运算次数每秒）来衡量运算速度。
- 吞吐量：系统在单位时间处理请求的数量。提高吞吐率可以更快完成任务或处理更多数据
- 响应时间：从用户发起请求到系统响应请求并得到结果的等待时间
# 系统总线
## 总线的基本概念
- 总线连接：各部件连到一组公共信息传输线上
- 分散连接：各部件之间使用单独连线
### 不同总线结构
- CUP为中心的双总线结构：影响CPU工作效率
![[CUP为中心的双总线结构.png]]
- 单总线结构：影响整机工作速度
![[单总线结构.png]]
- 以存储器为中心的双总线结构
![[以存储器为中心的双总线结构.png]]
## 总线的分类
### 片内总线
芯片内部的总线
### 系统总线
各部件之间的信息传输线
#### 数据总线
- 用来传输各功能部件之间的数据信息
- 双向传输总线
- 位数与机器字长、存储字长有关
#### 地址总线
- 用来指出数据总线上数据的地址、I/O设备地址
- 单向
- 位数与存储地址、I/O地址有关
#### 控制总线
- 用来发出各种控制信号
- 对任意控制线单向，对控制总线双向
### 通信总线
计算系系统之间或与其他系统之间的通信
## 总线特性及性能指标
### 总线特性
- 机械特性：总线的机械连接方式
- 电气特性：总线的每一根传输线上信号的传递方向和有效的电平范围
- 功能特性：总线中每根传输线的功能
- 时间特性：总线中的任一根线在什么时间内有效
### 总线性能指标
- 总线宽度：数据总线的根数
- 总线带宽：总线的数据传输速率
- 时钟同步\异步
- 总线复用：一条信号线上分时传送两种信号
- 信号线数：地址总线、数据总线和控制总线三种总线数的总和
- 总线控制方式：包括突发工作、自动配置、仲裁方式、逻辑方式、计数方式等
- 其他指标：如负载能力、电源电压 、总线宽度能否扩展等
### 总线标准
- ISA总线
- EISA总线
- VESA（VL-BUS）总线
- PCI总线
- AGP总线
- RS-232C总线
- USB总线
## 总线结构
### 单总线结构
- 慢
### 多总线结构
- 双总线结构：快、贵
![[双总线结构.png]]
- 三总线结构
![[三总线结构.png]]
![[三总线结构的又一形式.png]]
- 四总线结构
![[四总线结构.png]]
## 总线控制
### 总线判优控制
- 主设备：对总线有控制权
- 设备：响应主线发出的总线命令
- 判优控制：
	- 集中式
		- 链式查询：BS 总线忙、BR 总线请求、BG 总线同意
			- 优点：线少、代码量少、扩展性好
			- 缺点：抗毁性差、优先级不灵活
			![[链式查询方式.png|447]]
		- 计数器定时查询
			- 优点：优先级可改变、抗毁性提高
			- 缺点：线增加（成本增加）、扩展性较差
			![[计数器定时查询方式.png]]
		- 独立请求方式
			- 优点：优先级灵活、响应快
			- 缺点：线多、复杂、扩展性最差
			![[独立请求方式.png]]
	- 分布式
### 总线通信控制
- 目的：解决协调配合问题、
- 周期：
	1. 申请分配
	2. 寻址
	3. 传数
	4. 结束
- 方式：
	- 同步通信
		- 优点：是规定明确、统一，模块间的配合简单一致。
		- 缺点：总线的工作效率低，设计缺乏灵活性。 
		- 用于总线长度较短、各部件存取时间比较一致的场合。 
		- 总线传输周期越短，数据线的位数越多，总线的数据传输率越高。
		- 读命令传输周期：
			1. 主模块发地址。
			2. 主模块发读命令。
			3. 从模块提供数据。 
			4. 主模块撤销读命令，从模块撤销数据。 
		- 写命令传输周期：
			1. 主模块发地址。 
			2. 主模块提供数据。 
			3. 主模块发出写命令，从模块接收到命令后，必须在规定时间内将数据总线上的数据写到地址总线所指明的单元中。 
			4. 主模块撤销写命令和数据等信号。
	- 异步通信
		- 当主模块发出请求 (Request) 信号时，一直等待从模块反馈回来“响应 ”(Ac knowledge) 信号后才开始通信。要求主、从模块之间增加两条应答线（握手交互信号线 Handshaking) 
		![[异步通信三种方式.png]]
		- 异步串行通信的数据传送速率用波特率来衡量。波特率是指单位时间内传送二进制数据的位数，单位用 bps （位／秒）表示，记作波特。
	- 半同步通信
		- 增设了一条”等待 "(WAIT) 响应信号线
		- 优点：控制方式比异步通信简单，可靠性较高，同步结构较方便。
		- 缺点：系统工作的速度不高。
		- 适用于系统工作速度不高但又包含了由许多工作速度差异较大的各类设备组成的简单系统
		- 周期：
			1. 主模块发出地址信息。 
			2. 主模块发出命令。 
			3. 当 WAIT 为低电平时，进入等待
			4. 从模块提供数据。 
			5. 主模块撤销读命令，从模块撤销数据。
	- 分离式通信
		- 特点： 
			- 各模块欲占用总线使用权都必须提出申请。 
			- 在得到总线使用权后，主模块在限定的时间内向对方传送信息，采用同步方式传送，不再等待对方的回答信号。 
			- 各模块在准备数据的过程中都不占用总线，使总线可接受其他模块的请求。 
			- 总线被占用时都在做有效工作，不存在空闲等待时间，充分地利用了总线的有效占用
# 存储器

# 输入输出设备
## 概述
### 发展概况
- 早期：I/O设备与主存交换信息都必须通过CPU
- 接口模块和DMA阶段：设备通过接口模块与主机连接，计算机系统采用了总线结构
- 具有通道结构的阶段
- 具有处理机的阶段：独立于主机工作
### I/O系统的组成
- 软件：输入、输出、协调
	- I/O指令：操作码 命令码 设备码
		- 输入、输出、状态测试、某些操作指令
	- 通道指令：用以指明参与传送（ 入或读取）的数据组在主存中的首地址；指明需要传送的字节数或所传送数据组的末地址；指明 所选设备的设备码及完成某种操作的命令码
- 硬件
### I/O设备与主机的联系方式
- 编址方式
	- 统一编址：地址看作存储器地址的一部分
	- 不统一编址：I/O地址与存储器地址分开
- 寻址
- 传送方式
- 联络方式
	- 立即响应方式
	- 异步工作采用应答信号联络
	- 同步工作采用同步时标联络
- 设备与主机的连接方式
	- 辐射式和总线式
### I/O设备与主机信息传送的控制方式
![[主机控制IO设备的方式.png]]
- 程序查询方式：CPU通过程序不断查询I/O设备是否已做好准备
- 程序中断方式：CPU 在启动I/O设备后继续执行自身程序，只是当I/O设备准备就绪并向 CPU 发出中断请求后才予以响应、
- 直接存储器存取(DMA)方式：
	- 主存与 I/0 设备之间有一条数据通路，主存与 I/0 设备交换信息时，无须调用中断服务程序
	- 若出现 DMA 和 CPU 同时访问主存， CPU 总是将总线占有权让给 DMA, 通常把 DMA 的这种占有称为窃取或挪用
	- 窃取的时间一般为一个存取周期，故又把 DMA 占 用的存取周期窃取周期或挪用周期。
## I/O设备
### 概述
- 人机交互设备：键盘、鼠标、手写板、扫描仪、摄像机、语音识别、印机、显示器、绘图仪、语音合成器......
- 计算机信息的存储设备：盘、光盘、磁带
- 机-机通信设备：调制解调器 (Modem) 、 DIA 、 AID 转换设备
### 输入设备
### 输出设备
### 其他I/O设备
### 多媒体技术
## I/O接口
### 概述
-  I/0 接口通常是指主机与 I/0 设备之间设置的一个硬件电路及其相应的软件控制。
### 接口的功能和组成
- 总线路连接方式
	- 数据线：
		- 数据代码的传送线
		- 根数等于存储字长的位数或字符的位数
		- 通常是双向的，也可以是单向的。若采用单向数据总线，则必须用两组才能实现数据的输入和输出功能，而双向数据总线只需一组即可。
	- 设备选择线：
		- 传送设备码
		- 根数与设备码的位数有关。
		- 设备选择线可以有一组，也可以有两组，其中一组用于主机向 I/0 设备发送设备码，另一组用千 I/0 设备向主机回送设备码。当然设备选择 线也可采用一组双向总线代替两组单向总线。
	- 命令线：
		- 传输 CPU 向设备发出的各种命令信号，如启动、清除、屏蔽、读、写等。
		- 根数与命令信号多少有关
		- 它是一组单向总线
	- 状态线：
		- 状态线是将 I/0 设备的状态向主机报告的信号线，例如，设备是否准备就绪，是否向 CPU 发 出中断请求等。
		- 它也是一组单向总线。
- 接口的功能和组成：
	- 选址功能：设备选择线
	- 传送命令的功能：命令寄存器以及命令译码器
	- 传送数据的功能：数据缓冲寄存器 (Data Buffer Register, DBR)
	- 反映I/O设备工作状态的功能：中断请求触发器 INTR
### 接口类型
- 按数据传送方式分类，有并行接口和串行接口两类
- 按功能选择的灵活性分类，有可编程接口和不可编程接口两种
- 按通用性分类有通用接口和专用接口
- 按数据传送的控制方式分类，有程序型接口和 DMA 型接口
## 程序查询方式
### 程序查询流程
1. 将寄存器原内容保护起来
2. 设置 1/0 设备与主机交换数据的计数值。 
3. 设置欲传送数据在主存缓冲区的首地址。
4. CPU 启动 I/0 设备。 
5. 将 I/0 接口中的设备状态标志取至 CPU 并测试 I/0 设备是否准备就绪。当准备就绪时，接着可实现传送。对输入而言，准备就绪称为输入缓冲满；对输出而言，准备就绪称为输出缓冲空。 
6. CPU 执行 I/0 指令，或从 I/0 接口的数据缓冲寄存器中读出一个数据，或把一个数据写 入 I/0 接口中的数据缓冲寄存器内，同时将接口中的状态标志复位。 
7. 修改主存地址。 
8. 修改计数值，若原设置计数值为原码，则依次减 l ；若原设置计数值为负数的补码，则依次 加 1 （有关原码、补码的概念可参阅 6.1 节）。 
9. 判断计数值。若计数值不为 0 ，表示一批数据尚未传送完，重新启动外设继续传送；若计 数值为 0 ，则表示一批数据已传送完毕。 
10. 结束 I/0 传送，继续执行现行程序。
### 程序查询方式的接口电路
- 以输入设备为例，该接口的工作过程如下： 
	1. 当 CPU 通过 I/0 指令启动输入设备时，指令的设备码字段通过地址线送至设备选择 电路。
	2. 若该接口的设备码与地址线上的代码吻合，其输出 SEL 有效。 
	3. I/0 指令的启动命令经过”与非“门将工作触发器 B 置 ”1" ，将完成触发器 D 置 “O” 。 
	4. 由 B 触发器启动设备工作。 
	5. 输入设备将数据送至数据缓冲寄存器。 
	6. 由设备发设备工作结束信号，将 D 置 ”1", B 置 “0” ，表示外设准备就绪。
	7. D 触发器以“准备就绪＂状态通知 CPU ，表示“数据缓冲满”。 
	8. CPU 执行输入指令，将数据缓冲寄存器中的数据送至 CPU 的通用寄存器，再存入主存相 关单元
## 程序中断方式
### 中断的概念
- 计算机在执行程序的过程中，当出现异常情况或特殊请求时，计算机停止现行程序的运行， 转向对这些异常情况或特殊请求的处理，处理结束后再返回到现行程序的间断处，继续执行原程序，这就是“中断＂
### I/O中断的产生
### 程序中断方式的接口电路
- 中断请求器和中断屏蔽触发器
- 排队器
- 中断向量地址形成部件（设备编码器）
	- 它的输出是 中断向量（二进制代码表示），其位数与计算机可以处理中断源的个数有关，即一个中断源对 应一个向量地址
- 程序中断方式接口电路的基本形成
	- ![[程序中断方式接口电路.png]]
### I/O中断处理过程
- CPU响应中断的条件和时间：
	- 条件是必须满足 CPU 中的允许中断触发器 EINT 为 “1“ 
	- 时间一定是在每条指令执行阶段的结束时刻
- I/O中断处理过程：纳为中断请求、中断判优、中断响应、中断服务和 中断返回 5 个阶段。
	1. 由 CPU 发启动 I/0 设备命令，将接口中的 B 置 “1", D 置 “O” 。 
	2. 接口启动输入设备开始工作。 
	3. 输入设备将数据送入数据缓冲寄存器。 
	4. 输入设备向接口发出“设备工作结束“信号，将 D 置 ”1", B 置 “O” ，标志设备准备就绪。 
	5. 当设备准备就绪 (D = 1) ，且本设备未被屏蔽 (MASK= 0) 时，在指令执行阶段的结束时 刻，由 CPU 发出中断查询信号。 
	6. 设备中断请求触发器 INTR 被置 “1” ，标志设备向 CPU 提出中断请求。与此同时， INTR 送至排队器，进行中断判优。 
	7. 若 CPU 允许中断 (EINT= 1) ，设备又被排队选中，即进入中断响应阶段，由中断响应信号 INTA 将排队器输出送至编码器形成向量地址。 
	8. 向量地址送至 PC ，作为下一条指令的地址。 
	9. 由于向量地址中存放的是一条无条件转移指令，故这条指令执行结束后， 即无条件转至该设备的服务程序入口地址，开始执行中断服务程序，进入中断服务阶段，通过输 入指令将数据缓冲寄存器的输入数据送至 CPU 的通用寄存器，再存入主存相关单元。 
	10. 中断服务程序的最后一条指令是中断返回指令，当其执行结束时，即中断返回至原程序 的断点处。
### 中断服务程序的流程
1. 保护现场
2. 中断服务
3. 恢复现场
4. 中断返回
# 计算机的运算方法
## 无符号数和有符号数
### 无符号数
- 默认符号为正
- 寄存器位数=无符号数表示范围（0~$2^n-1$）
### 有符号数
- 机器数：用01表示符号，转换后的数
- 真值：带符号的数
- 原码($[X_原]$)：
	- 符号位为 0 表示正数，符号位为 1 表示负数，数值位即真值的绝对值。
	- 约定整数的符号位与数值位之间用逗号隔开；
	- 小数的符号位与数值位之间用小数点隔开。
	- 真值数量：$2^{n-1}-1$，因为0=-0，产生了重合。
	- 范围：-($2^{n-1}-1$)~($2^{n-1}-1$)
- 补码($[X_补]$)：
	- 负数：反码+1
	- 正数：不变
	- 真值数量：$2^{n-1}$
	- 范围：-($2^{n-1}$)~($2^{n-1}-1$)
	- $[-X_补]$=$[X_补]$各位取反，再+1
- 反码($[X_反]$)：
	- 负数：符号位不变，真值取反
	- 正数：不变
	- 真值数量、范围：同原码
- 移码：
	- 常用来表示浮点数的阶码
	- 真值+常数（通常为$2^{n-1}$）/补码符号位取反
	- 真值数量、范围：同补码
## 数的定点表示和浮点表示
### 定点表示
- 小数点固定在某一位置，数符后（小数）或数值后（整数）
- 由于小数点的位置固定不变，故当机器处理的数不是纯小数或纯整数时，必须乘上一个比例因子，否则会产生＂溢出”。
- 整数范围：0~$2^n-1$
- 小数范围：-(1-$2^{-(n-1)}$)~(1-$2^{-(n-1)}$）
### 浮点表示
- 小数点位置可以浮动的数
- $N=S\times r^i$，S为尾数，j为阶码，r是基数（可取2,4,8,16）
- 阶码是整数，阶符和阶码的位数 m 合起来反映浮点数的表示范围及小数点的实际位置；尾数是小数，其位数 n 反映了浮点数的精度；尾数的符号 $S_f$代表浮点数的正负
- 表示范围：设阶码m位，尾数n位；
	- 正数：$2^{-(2^m-1)}\times(1-2^{-n})$到$2^{(2^m-1)}\times(1-2^{-n})$
	- 负数：$-2^{(2^m-1)}\times(1-2^{-n})$到$-2^{-(2^m-1)}\times(1-2^{-n})$
- 规格化：
	- 当基数为2时，尾数最高位为1的数为规格化数。规格化时，尾数左移一位，阶码减1（左规）；尾数右移一位，阶码加1（右规）
	- 当基数为4时，尾数的最高两位不全为零的数为规格化数。规格化时，尾数左移两位，阶码减1；尾数右移两位，阶码加1。
### 定点数和浮点数的比较
- 相同位数，浮点数范围更大
- 规格化后，浮点数精度更高
- 浮点数运算步骤多、速度低、线路复杂
- 溢出判断：浮点数判断规格化数的阶码判断；定点数对数据本身判断
## 定点运算
- 移位、加、减、乘、除
### 移位运算
- 算术移位（有符号数）
	- 一般用补码，符号位不动
	- 左移：缺位补0，溢出位丢掉；可能溢出
	- 右移：缺位补符号位，溢出位丢掉；可能影响精度
	- 原码：补0，反码：补符号位
- 逻辑移位：
	- 缺位补0，溢出位丢掉
- x左移k位：x<<k;
- x右移k位：x>>k;
### 加减法
- 加法：
	- 整数：$[A]_补＋[B]_补＝[A+B]_补(mod 2^{n+1})$ 
	- 小数：$[A]_补＋[B]_补＝[A+B]_补(mod 2)$
- 减法：
	- 整数：$[A-B]_补=[A]_补＋[-B]_补(mod 2^{n+1})$
	- 小数：$[A-B]_补=[A]_补＋[-B]_补(mod 2)$
- 溢出判断：操作数符号相同，结果又与原操作数的符号不同
- $[-X_补]$=$[X_补]$各位取反，再+1
### 乘法
- 原码一位乘法：
	- 结果的符号位：乘数符号位异或
	- 结果的数值位：由乘数数值部分相乘（无符号数相乘）
	- 无符号数相乘：$X\times Y=\sum_{i=1}^n(X\times Y_i\times2^{-i})$
- 补码乘法：
	- 比较法（booth算法）
	- 补码两位乘
### 除法运算
- 原码除法：
	- 恢复余数法：
	- 加减交替法：
- 补码除法：
	- 加减交替法：
## 浮点四则运算
### 加减运算
- 步骤：
	1. 对阶，使两数的小数点位置对齐。
	2. 尾数求和，将对阶后的两尾数按定点加减运算规则求和（差）。
	3. 规格化，为增加有效数字的位数，提高运算精度，必须将求和（差）后的尾数规格化。
	4. 舍入，为提高精度，要考虑尾数右移时丢失的数值位。
	5. 溢出判断，即判断结果是否溢出。 
### 乘除法运算
- 阶码运算
- 尾数运算
## 算术逻辑单元（ALU）
### ALU电路
![[ALU电路.png]]
![[ALU电路续表.png]]
### 快速进位链
- 并行加法器
	- ![[并行加法器.png]]
	- $C_i=A_iB_i+(A_i+B_i)C_{i-1}=d_i+t_iC_{i-1}$
- 串行进位链
	-  ![[串行进位链.png]]
	- n位全加器最长进位时间：$2nt_y$
- 并行进位链
	- 单重分组跳跃进位
		- ![[单重分组跳跃进位.png]]
		- n组进位时间：$2.5nt_y$
	- 双重分组
		- ![[双重分组跳跃进位.png]]
		- 
# 指令系统
## 机器指令
### 一般格式
- 操作码+地址码
- 操作码：位数反映了允许指令长度，$2^n$条指令
- 地址码：
	- 四地址指令：第一操作数地址、第二操作数地址、结果地址、下一指令地址 
	- 三地址指令：用PC得到下一指令地址，则第一操作数地址、第二操作数地址、结果地址
	- 二地址指令：结果暂存CPU，第一操作数地址、第二操作数地址
	- 一地址指令：一个操作数地址隐含在ACC中，操作数地址
	- 零地址指令：个别指令如空操作、停机、中断返回......
### 指令字长
- 指令字长取决于操作码的长度、操作数地址的长度和操作数地址的个数
- 通常指令字长取 8 的整数倍
## 操作数类型和操作类型
### 操作数类型
- 地址、数字、字符、逻辑数据
### 存储器中的存放方式
![[数据的存放方式.png]]
### 操作类型
- 数据传送、算术逻辑运算、移位、转移、输入输出、其他
- 转移：无条件转移、条件转移、调用与返回、陷阱与陷阱指令
## 寻址方式
### 指令寻址
- 顺序寻址、跳跃寻址
### 数据寻址
- 地址码=寻址特征+形式地址A
- 真实地址EA由寻址特征与形式地址A确定
- 立即寻址：
	- 特征：#
	- A是操作数本身
- 直接寻址：
	- EA=A
- 隐含寻址：
	- 操作数的地址隐藏在操作码或寄存器中
- 间接寻址：
	- A指向EA的地址
	- EA=(A)
- 寄存器寻址：
	- 地址码字段直接指出寄存器编号
	- EA=R
- 寄存器间接寻址：
	- 寄存器中位操作数地址
	- EA=(R)
- 基址寻址：
	- 基址寄存器BR
	- EA=A+(BR)
	- 隐式、显式
	- 内容由操作系统/管理程序确定
- 变址寻址：
	- 变址寄存器IX
	- EA=A+(IX)
	- 内容由用户设定，程序执行过程中值可变
- 相对寻址：
	- PC的内容即当前指令地址+A
	- EA=(PC)+A
	- 常被用于转移类指令，A为位移量，通常为补码
- 堆栈寻址：
	- 用寄存器组（硬堆栈）/主存（软堆栈）实现
	- 堆栈指针SP指向栈顶
	- 本质可视为寄存器间接寻址，EA=SP
## 指令格式举例
### 设计指令格式的因素
- 操作类型：包括指令数及操作的难易程度
- 数据类型：确定哪些数据类型可以参与操作
- 指令格式：包括指令字长、操作码位数、地址码位数、地址个数、寻址方式类型，以及指令字长和操作码位数是否可变等 
- 寻址方式：包括指令和操作数具体有哪些寻址方式 
- 寄存器个数：寄存器的多少直接影响指令的执行时间
### 指令格式举例
- PDP-8：
	- 指令字长统一12位
	- ![[PDP-8.png]]
	- 共35条指令
- PDP-11：
- IBM 360：
- Intel 8086/80486
## RISC技术
- RISC：精简指令系统计算机
- CISC：复杂指令系统计算机
### 产生和发展
- RISC 技术是用20％的简单指令的组合实现不常用的80％指令功能
### 主要特征
- RISC Ⅱ指令系统
	- 指令种类：
		- 寄存器－寄存器操作：移位、逻辑、算术（整数）运算等 12 条。 
		- 取／存数指令：取存字节、半字、字等 16 条。 
		- 控制转移指令：条件转移、调用／返回等 6 条。 
		- 其他：存取程序状态字 PSW 和程序计数器等 5 条。
	- 指令格式：
		- 指令字长固定32位
		- ![[RISC Ⅱ指令格式.png]]
	- 寻址方式：变址寻址、相对寻址
### RISC和CISC的比较
- 优点：
	- 充分利用VLSI芯片面积
	- 提高计算机运算速度
	- 便于设计、降低成本、提高可靠性
	- 有效支持高级程序语言
# CPU的结构和功能
## CPU的结构
### CPU的功能
- 取指令、分析指令、执行指令
### CPU结构框图
![[CPU结构图.png]]
### CPU的寄存器
- 用户可见寄存器（用户可编程）
	- 通用寄存器
	- 数据寄存器
	- 地址寄存器
	- 条件码寄存器
- 控制和状态寄存器（不可编程）
	- MAR：存储器地址寄存器，用于存放将被访问的存储单元的地址。
	- MDR：存储器数据寄存器，用于存放欲存入存储器中的数据或最近从存储器中读出的数据。
	- PC：程序计数器，存放现行指令的地址，通常具有计数功能。
	- IR：指令寄存器，存放当前欲执行的指令。
### 控制单元和中断系统
## 指令周期
### 指令周期的基本概念
- 指令周期：CPU 每取出并执行一条指令所需的全部时间称为指令周期，也即 CPU 完成一条指令的时间。
	- 取指周期：取指阶段完成取指令和分析指令的操作。
	- 执行周期：执行阶段完 成执行指令的操作，又称执行周期。
### 指令周期的数据流
- 取指周期的数据流：
![[取指周期数据流.png]]
- 间指周期数据流：
![[间指周期数据流.png]]
- 执行周期数据流：多样
- 中断周期数据流：
![[中断周期数据流.png]]
## 指令流水
- 用于提升处理机的操作级和指令级的并行性
### 指令流水原理
- 通常为了减少时间损失，采用猜测法，即当条件转移指令从取指阶段进入执行阶段时，指令部件仍按顺序预取下一条指令。
- 可将指令的处理过程分解为更细的几个阶段：
	- 取指 (FI) ：从存储器取出一条指令并暂时存入指令部件的缓冲区。 
	- 指令译码 (DI) ：确定操作性质和操作数地址的形成方式。 
	- 计算操作数地址 (CO) ：计算操作数的有效地址，涉及寄存器间接寻址、间接寻址、变址寻址、基址寻址、相对寻址等各种地址计算方式。 
	- 取操作数 (FO) ：从存储器中取操作数（若操作数在寄存器中，则无须此阶段）。 
	- 执行指令（ EI) ：执行指令所需的操作，并将结果存于目的位置（寄存器中）。 
	- 写操作数 (WO) ：将结果存入存储器。
![[指令的六级流水时序.png]]
### 影响流水线性能的因素
- 结构相关：硬件资源不足
	- 设置两个独立的存储器分别存放操作数和指令
- 数据相关：后继指令需要用到前面指令的执行结果
	- 后推法，等待前面指令的结果生成。
	- 定向技术，直接将执行结果送到其他指令所需要的地方
- 控制相关：分支指令或改变PC指令
	- 尽早判别转移是否发生，尽早生成转移目标地址
	- 预取转移成功或不成功两个控制流方向上的目标指令
	- 加快和提前形成条件码
	- 提高转移方向的猜准率等方法。
### 流水线性能
- 吞吐率：指单位时间内流水线所完成指令或输出结果的数量
	- 最大吞吐率：连续流动达到稳定状态后的吞吐量
	- 实际吞吐率：平均吞吐率
- 加速比：m 段流水线的速度与等功能的非流水线的速度之比
- 效率：流水线中各功能段的利用率
### 流水线中的多发技术
- 超标零技术：在每个时钟周期内可同时并发多条独 立指令，即以并行操作方式将两条或两条以上（图中所示为 3 条）指令编译并执行。
- 超流水线技术：将一些流水线寄存器插入流水线段中，好比将流水线再分段
- 超长指令字技术：在一个时钟周期内能流出多条指令
### 流水线结构
- 指令流水线结构
- 运算流水线
## 中断系统
### 概述
- 引起中断的因素
	- 人为设置的中断
	- 程序性事故
	- 硬件故障
	- I/O设备
	- 外部事件
- 中断系统需解决的问题
	- 中断源如何向 CPU 提出中断请求。 
	- 当多个中断源同时提出中断请求时，中断系统如何确定优先响应哪个中断源的请求。
	- CPU 在什么条件、什么时候、以什么方式来响应中断。 
	- CPU 响应中断后如何保护现场。 
	- CPU 响应中断后，如何停止原程序的执行而转入中断服务程序的入口地址。
	- 中断处理结束后， CPU 如何恢复现场，如何返回到原程序的间断处。 
	- 在中断处理过程中又出现了新的中断请求， CPU 该如何处理。
### 中断请求标记和中断判优逻辑
- 中断请求标记：
	- 中断请求标记触发器，简称中断请求触发器，记作 INTR 。1表示中断源有请求
	- ![[中断请求标记寄存器.png]]
- 中断判优逻辑
	- 各中断源的优先顺序是根据该中断源若得不到及时响应，致使机器工作出错的严重程度而定的。越严重优先度越高。
	- 硬件排队：链式排队器，CPU中的排队器
	- 软件排队
### 中断服务程序入口地址的寻找
- 硬件向量法：利用硬件产生向量地址，再由向量地址找到中断服务程序的入口地址
- 软件查询法
### 中断响应
- 响应中断的条件
- 响应中断的时间
- 中断隐指令（中断周期内CPU的操作）
	- 保护程序断点：保存PC
	- 寻找中断服务程序的入口地址
	- 关中断
### 保护现场和恢复现场
### 中断屏蔽技术
- 多重中断的概念：执行中断时又有新的中断请求，CPU暂停旧中断程序去执行新的中断程序
- 多重中断的条件
	- 提前设置开中断指令
	- 优先级别高的中断源有权中断优先级别低的中断源
- 屏蔽技术：保证低级中断源不干扰高级中断源的中断过程
	- 屏蔽触发器与屏蔽字
	- 屏蔽技术可改变优先等级
- 多重中断的断点保护
	- 在中断周期内由中断隐指令实现的，对用户是透明的
	- 断点可以保存在堆栈中，也可保存在特定的存储单元内
# 控制单元的功能
# 控制单元的设计
