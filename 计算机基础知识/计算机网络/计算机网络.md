# 计算机网络体系结构
## 分层模型
### OSI模型
- 七层协议：物理层、数据链路层、网络层、运输层、会话层、表示层以及应用层
- 物理层：在传输媒体上透明地传输**比特流**
- 数据链路层：在相邻节点之间的链路上传输**数据帧**，帧包含数据和必要的控制信息（如地址信息、差错控制等），可以实现相邻节点的流量控制。
- 网络层：
- 运输层：
- 会话层：
- 表示层：
- 应用层：
### TCP/IP模型
### 实体、协议和服务